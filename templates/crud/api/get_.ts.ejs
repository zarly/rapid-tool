import { Request, Response } from 'express';
import { <%= name.camelCase %> } from 'src/models/<%= name.snakeCase %>';

export async function handler (req: Request, res: Response) {
    const data = await <%= name.camelCase %>.list();
    res.json(data);
}

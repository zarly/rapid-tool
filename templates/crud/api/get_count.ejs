import { Request, Response } from 'express';
import { <%= name.camelCase %> } from '<%= srcRoot %>/models/<%= name.snakeCase %>';

export async function handler (req: Request, res: Response) {
    const count = await <%= name.camelCase %>.count();
    res.json(count);
}

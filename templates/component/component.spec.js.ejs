import Vue from 'vue';
import { expect } from 'chai';
import Component from './<%- name.snakeCase %>';

describe('<%- name.classCase %>', function () {
  it('should renders without errors', () => {
    const Constructor = Vue.extend(Component);
    const vm = new Constructor().$mount();
  });
});

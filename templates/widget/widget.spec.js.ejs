import Vue from 'vue';
import { expect } from 'chai';
import Widget from './<%- name.snakeCase %>';

describe('<%- name.classCase %>', function () {
  it('should renders without errors', () => {
    const Constructor = Vue.extend(Widget);
    const vm = new Constructor().$mount();
  });
});
